# Copy to `.env.local` (gitignored) for local overrides. Vite loads `.env.local` automatically.
#
# When testing from a phone on the same Wi‑Fi, set your Mac’s LAN IP (see `ipconfig getifaddr en0`):
# VITE_API_URL=http://192.168.1.42:3000/api

# Google Maps / Places API
VITE_GOOGLE_PLACES_KEY=
